Was macht SPxSolver in example. Warum können wir nicht einfach SoPlex 
benutzen?

Warum kann ich in DataKey.h nicht den expliziten CopyConstruktor einführen?

in idlist.h die subclassen einsetzen.

BUG in spxscalesm.cc value() wird nicht descaliert. 
Entsprechender Aufruf fehlt auch in spxgeneralsm.cc

In slufactor.cpp sind lauter cast auf CLUFactor* drin, die
man wohl wennehmen könnte, wenn man in solve.c und vsolve.c überall
const hinschreibt.

long durch ptrdiff_t ersetzen (include <stddef.h>

for();    -> for()
                ;
assert(xxx) -> assert(xxx != 0)

explicit einfuegen.

vector_c.cpp in vector.cpp einbauen (this)

documentieren  doc++

reinterpret_cast durchsehen

svector neu implementieren. Den [-1] Zugriff erstetzen durch
class SVector
{
   int     size;
   int     used;
   int*    idx;
   double* val;
};
Es wird viel Platz verschwendet durch das zusammensetzen von
Element { int, double } (padding),
Ist vermutlich dann auch schneller.
Es muessen dann aber auch DSVector und SVSet überarbeitet werden.


// lprow.cpp:double LPRow::infinity = 1e+100;
// spxlp.cpp:const double SPxLP::infinity = 1e+100;

SORTIERUNG 

   daten
   public daten werden ersetzt.
   -------
   public
   protected
   private

   constructor/assignment operator (alle typen)
   destructor
   initialisierung


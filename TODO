ctimer/timer integrieren.

Netlib orginal holen und leseroutinen testen. maros-r7 ist nicht im test.
andere auch?

LPSolver zusammenstutzen oder ganz entfernen.

in dataarray.h:reSize(int newsize) steht in der docxygen docu immer int n ???

spxfastrt.cpp(865) Testen ob das ; korrekt ist.

Was macht SPxSolver in example. Warum können wir nicht einfach SoPlex 
benutzen?

Warum kann ich in DataKey.h nicht den expliziten CopyConstruktor einführen?

in idlist.h die subclassen einsetzen.

BUG in spxscalesm.cc value() wird nicht descaliert. 
Entsprechender Aufruf fehlt auch in spxgeneralsm.cc

for();    -> for()
                ;
explicit einfuegen.

reinterpret_cast durchsehen

// lprow.cpp:double LPRow::infinity = 1e+100;
// spxlp.cpp:const double SPxLP::infinity = 1e+100;

SORTIERUNG 

   daten
   public daten werden ersetzt.
   -------
   public
   protected
   private

   constructor/assignment operator (alle typen)
   destructor
   initialisierung


Dokumentation: (alle * sind bereits fertig)

cring.h
clufactor.h
+slufactor.h  // hier fehlen noch ein paar Sachen  -> Thorsten!
+soplex.h // in Arbeit ;-)


